# Changelog

{{ changelog_insertion_flag }}

{% for release in releases %}
## {{ release.version }} - {{ release.date.strftime('%Y-%m-%d') }}

{% set priority_order = ['feat', 'fix', 'build', 'ci', 'refactor', 'perf', 'chore', 'docs', 'style', 'test', 'revert'] %}

{% for type in priority_order %}
  {% for section in release.sections if section.type == type %}
### {{ section.type | capitalize }}

    {% for commit in section.commits %}
- {{ commit.subject }}
    {% endfor %}
  {% endfor %}
{% endfor %}

{% endfor %}
