## {{ version.as_tag() }} Release

Released on {{ tagged_date.strftime("%Y-%m-%d") }}

{% for type_, commits in elements.items() if type_ != "unknown" | dictsort %}
### {{ type_ | title }}

{% for commit in commits %}
* {{ commit.descriptions[0] | capitalize }} ([`{{ commit.hexsha[:7] }}`]({{ commit.hexsha | commit_hash_url }}))
{% endfor %}

{% endfor %}
